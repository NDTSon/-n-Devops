apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: interaction-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
  name: interaction-db
  replicas: 1
  selector:
    matchLabels:
      app: interaction-db
  template:
    metadata:
      labels:
        app: interaction-db
    spec:
      containers:
        - name: interaction-db
          image: postgres:18-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: interaction_service_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
                  key: POSTGRES_PASSWORD
            - name: TZ
                configMapKeyRef:
                  name: app-config
                  key: TZ
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: interaction-db-data
              mountPath: /var/lib/postgresql
      volumes:
        - name: interaction-db-data
          persistentVolumeClaim:
            claimName: interaction-db-pvc
kind: Service
    app: interaction-db
  ports:
    - port: 5432
      targetPort: 5432
