server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/file_service_db
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:your_local_postgres_password}

seaweedfs:
  master:
    url: http://localhost:9333
  filer:
    url: http://localhost:8889
